class TwoSum {
    public numbers!: number[];
    public target!: number;
    public result!: number[];

    public constructor(numbers: number[], target: number) {
        this.numbers = numbers;
        this.target = target;

        for (let i:number = 0; i < this.numbers.length; i++) {
        }
    }
}


export default TwoSum;